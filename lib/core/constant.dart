enum MenuKey { Dashboard, Transaction, Casher, Stock, Store, Notification, Setting, Profile }

enum CategoryKey { Food, Drink, Snack }

enum PaymentMethodKey { Cash, Debit, Qris }

enum PostTag { Views, Dark, Sky }
